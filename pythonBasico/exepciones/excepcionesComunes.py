""""
Errores estándar más frecuentes
ValueError: Ocurre cuando una función recibe un valor incorrecto pero del tipo esperado.

Ejemplo: int("abc")

ZeroDivisionError: Se produce al intentar dividir entre cero.

Ejemplo: 10 / 0

TypeError: Ocurre cuando se realiza una operación con tipos incompatibles.

Ejemplo: "abc" + 123

IndexError: Ocurre al intentar acceder a un índice que no existe en una lista o tupla.

Ejemplo: [1, 2, 3][10]

KeyError: Sucede cuando intentas acceder a una clave inexistente en un diccionario.

Ejemplo: diccionario["clave_inexistente"]

AttributeError: Se produce al intentar acceder a un atributo que no pertenece a un objeto.

Ejemplo: 1234.append(5)

FileNotFoundError: Ocurre cuando intentas abrir un archivo que no existe.

Ejemplo: open("archivo_inexistente.txt", "r")
"""